<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppGridItem" parent="@style/Widget.AppCompat.ImageButton">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#008</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_rowWeight">1</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:padding">3dp</item>
        <item name="android:layout_marginStart">0dp</item>
<!--        <item name="android:layout_marginStart">3dp</item>-->
<!--        <item name="android:padding">16dp</item>-->
        <item name="android:scaleType">fitCenter</item>
        <!--        <item name="app:srcCompat">@drawable/transparent</item>-->
    </style>

    <style name="AppGridItemDark" parent="@style/AppGridItem">
        <item name="android:background">@drawable/transparent</item>
    </style>

    <style name="AppGridItemLight" parent="@style/AppGridItem">
        <item name="android:background">@drawable/transparent</item>
    </style>

    <style name="my_dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="windowFixedWidthMajor">70%</item>
        <item name="windowFixedWidthMinor">70%</item>
    </style>

</resources>
